apiVersion: v1
kind: Service
metadata:
  name: k8s-helloworld
spec:
  selector:
    app: helloworld
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloworld
  labels:
    app: helloworld
spec:
  replicas: 1
  selector:
    matchLabels:
      app: helloworld
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      containers:
      - name: server
        image: ghcr.io/robert-rino/heroku-alpinehelloworld:sha-02203f3
        env:
        - name: PORT
          value: '8080'
        ports:
        - containerPort: 8080
          protocol: TCP
          name: server
